---
description: Rank a name or screen the universe on factor/residual risk; optional residual-reversion signal on a basket
argument-hint: "<ticker or basket e.g. NVDA AMD AVGO MU>"
---

# Residual screen

Screen `$ARGUMENTS` using the **residual-screen** skill.

1. For one ticker: `riskmodels_get_rankings`. For "top/bottom N": `riskmodels_screen_rankings`. For a basket signal: `riskmodels_get_residual_signal`.
2. Lead with the one-line rank or signal; put tables below.
3. Describe ranks and signals as model outputs — never as buy/sell recommendations.

Realized/historical only — not investment advice.
